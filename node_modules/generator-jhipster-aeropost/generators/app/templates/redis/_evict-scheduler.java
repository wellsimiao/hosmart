package <%=packageName%>.config.evict;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

import net.javacrumbs.shedlock.core.SchedulerLock;

@Component
public class EvictScheduler {
	
	private CacheManagerEvict cacheManagerEvict;
	
	public EvictScheduler(CacheManagerEvict cacheManagerEvict) {
		this.cacheManagerEvict = cacheManagerEvict;
	}

	private static final Logger LOGGER = LoggerFactory.getLogger(EvictScheduler.class);

	@SchedulerLock(name = "cache_evict")
	@Scheduled(cron = "${application.cacheCronEvict}")
	public void refreshCaches() {
		
		LOGGER.info("Invalidating caches");
		cacheManagerEvict.evictAllCachesKeys();
	}
}
