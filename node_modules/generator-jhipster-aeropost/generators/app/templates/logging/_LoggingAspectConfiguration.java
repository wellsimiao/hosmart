package <%=packageName%>.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.EnableAspectJAutoProxy;
import org.springframework.context.annotation.Profile;
import org.springframework.core.env.Environment;

import <%=packageName%>.aop.logging.LoggingAspect;
import <%=packageName%>.aop.logging.LoggingAspectProd;

import io.github.jhipster.config.JHipsterConstants;

@Configuration
@EnableAspectJAutoProxy
public class LoggingAspectConfiguration {

	@Bean
	public LoggingAspect loggingAspect(Environment env) {
		return new LoggingAspect(env);
	}

	@Profile({JHipsterConstants.SPRING_PROFILE_PRODUCTION, Constants.SPRING_PROFILE_INTEGRATION, Constants.SPRING_PROFILE_STAGE})
	public LoggingAspectProd loggingAspectProd(Environment env) {
		return new LoggingAspectProd(env);
	}
}

